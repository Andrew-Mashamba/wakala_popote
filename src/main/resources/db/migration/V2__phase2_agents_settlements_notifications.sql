-- Phase 2: agents, agent_assignments, settlements, notification_log
-- Schema is created by JPA ddl-auto in dev/test. For production, add full DDL here
-- and set spring.jpa.hibernate.ddl-auto=validate (see BACKEND_IMPLEMENTATION_PLAN).

SELECT 1;
