-- Phase 1 baseline (BACKEND_IMPLEMENTATION_PLAN.md)
-- Schema is managed by JPA ddl-auto=update for dev. This migration establishes Flyway history.
-- For production, add full DDL here and set spring.jpa.hibernate.ddl-auto=validate.

SELECT 1;
